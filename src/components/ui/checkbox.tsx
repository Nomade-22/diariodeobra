"use client";
import * as React from 'react';import {Check} from 'lucide-react';import {cn} from '@/lib/utils';export function Checkbox({checked,onCheckedChange,className,disabled}:{checked?:boolean;onCheckedChange?:(v:boolean)=>void;className?:string;disabled?:boolean}){return <button type="button" role="checkbox" aria-checked={!!checked} disabled={disabled} onClick={()=>onCheckedChange?.(!checked)} className={cn('h-5 w-5 shrink-0 rounded border border-input flex items-center justify-center',checked&&'bg-primary text-primary-foreground border-primary',className)}>{checked&&<Check className="h-4 w-4"/>}</button>;}
