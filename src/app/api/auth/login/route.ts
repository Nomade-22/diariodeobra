import { NextResponse, type NextRequest } from "next/server";
import sql from "@/app/api/utils/sql";
import { OBRA_SESSION_COOKIE, OBRA_SESSION_MAX_AGE, generateSessionToken, hashPassword, obraSessionCookieOptions, readJson, rows, type ObraUserRow } from "@/app/api/_helpers/obra-auth";
export const dynamic="force-dynamic";
export async function POST(req:NextRequest){const body=await readJson(req);const username=typeof body.username==="string"?body.username:"";const password=typeof body.password==="string"?body.password:"";if(!username||!password)return NextResponse.json({error:"Usuário e senha são obrigatórios"},{status:400});const found=rows<ObraUserRow>(await sql`SELECT id::int AS id, username, nome, is_admin::int AS is_admin FROM app_users WHERE username=${username} AND password_hash=${hashPassword(password)} LIMIT 1`);const user=found[0];if(!user)return NextResponse.json({error:"Usuário ou senha inválidos"},{status:401});const res=NextResponse.json({id:user.id,username:user.username,nome:user.nome,is_admin:user.is_admin===1});res.cookies.set(OBRA_SESSION_COOKIE,`${user.id}:${generateSessionToken()}`,{...obraSessionCookieOptions,maxAge:OBRA_SESSION_MAX_AGE});return res;}
