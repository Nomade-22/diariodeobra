"use client";
import Layout from '@/components/Layout';import {Card,CardContent} from '@/components/ui/card';import {Button} from '@/components/ui/button';import {History,Loader2,Trash2,Clock} from 'lucide-react';import {useRegistros} from '@/hooks/useApi';
export default function HistoricoPage(){const{registros,loading,remove}=useRegistros();if(loading)return <Layout adminOnly><div className="flex justify-center py-20"><Loader2 className="w-8 h-8 animate-spin text-primary"/></div></Layout>;return <Layout adminOnly><div className="space-y-6"><div className="flex items-center gap-3"><div className="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center"><History className="w-6 h-6 text-primary"/></div><div><h2 className="text-2xl font-bold">Histórico</h2><p className="text-muted-foreground">{registros.length} registro(s)</p></div></div><div className="grid gap-4">{registros.map(r=><Card key={r.id}><CardContent className="p-5"><div className="flex justify-between gap-4"><div><div className="font-semibold text-lg">{r.cliente_nome||'Sem cliente'}</div><div className="text-sm text-muted-foreground flex gap-3 mt-1"><span>{r.data}</span><span className="flex items-center gap-1"><Clock className="w-3 h-3"/>{r.chegada||'--'} — {r.saida||'--'}</span></div><p className="mt-3">{r.trabalho}</p>{r.created_by&&<p className="text-sm text-muted-foreground mt-2">Responsável: {r.created_by}</p>}<p className="text-sm mt-2">{r.funcionarios?.map(f=>f.nome).join(', ')}</p></div><Button variant="ghost" size="icon" onClick={()=>confirm('Excluir este registro?')&&remove(r.id)}><Trash2 className="w-4 h-4 text-destructive"/></Button></div></CardContent></Card>)}</div></div></Layout>;}
